import { useEffect, useMemo, useRef, useState } from 'react';
import { Send, X, Loader2, Volume2, VolumeX, ArrowUpRight } from 'lucide-react';
import { motion, AnimatePresence } from 'motion/react';
import { useNavigate } from 'react-router-dom';
import axios from 'axios';
import { useConfig } from '../context/ConfigContext';
import { useAppContext, convertCurrency } from '../context/AppContext';
import { BRAND, BRAND_LOGO_URL } from '../config/brand';
const textOf=(value:any,fallback='')=>{if(typeof value==='string'||typeof value==='number')return String(value);if(value&&typeof value==='object')return String(value.name||value.title||value.text||value.content||fallback);return fallback};
export default function AIStylistPro(){const[isOpen,setIsOpen]=useState(false);const[input,setInput]=useState('');const[messages,setMessages]=useState<{role:'user'|'ai';text:string}[]>([]);const[isLoading,setIsLoading]=useState(false);const[isAudioEnabled,setIsAudioEnabled]=useState(false);const{SiteConfig}=useConfig();const{products,currency}=useAppContext();const navigate=useNavigate();const messagesEndRef=useRef<HTMLDivElement>(null);const liveProducts=useMemo(()=>Array.isArray(products)?products.filter((p:any)=>p&&(p.id||p._id)&&(p.name||p.title)).slice(0,30):[],[products]);const trending=useMemo(()=>[...liveProducts].sort((a:any,b:any)=>Number(Boolean(b.isTrending||b.isFeatured||b.isNewArrival))-Number(Boolean(a.isTrending||a.isFeatured||a.isNewArrival))).slice(0,4),[liveProducts]);const hasUserMessage=messages.some(m=>m.role==='user');const quickSuggestions=useMemo(()=>[...new Set([...trending.slice(0,3).map((p:any)=>`Show ${textOf(p.name||p.title,'this piece')}`),...(trending.some((p:any)=>p.isNewArrival)?['New arrivals']:[]),'Help me choose'])].slice(0,5),[trending]);useEffect(()=>{if(messages.length===0&&SiteConfig)setMessages([{role:'ai',text:`Hello. I’m your ${BRAND.name} shopping assistant. I can help you discover pieces, compare styles and navigate the live store.`}])},[SiteConfig,messages.length]);useEffect(()=>{messagesEndRef.current?.scrollIntoView({behavior:'smooth'})},[messages,isLoading]);const speak=(text:string)=>{if(!isAudioEnabled||!window.speechSynthesis)return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text.slice(0,500));window.speechSynthesis.speak(u)};const productHaystack=(p:any)=>[textOf(p.name),textOf(p.title),textOf(p.category),textOf(p.collectionName),textOf(p.description),...(Array.isArray(p.colors)?p.colors:[]),...(Array.isArray(p.tags)?p.tags:[])].map(textOf).filter(Boolean).join(' ').toLowerCase();const findProduct=(query:string)=>{const q=query.toLowerCase().replace(/\b(show|open|find|give|me|please|the|a|an|product|item)\b/g,' ').replace(/\s+/g,' ').trim();if(!q)return null;const terms=q.split(/\s+/).filter(Boolean);return liveProducts.map((p:any)=>{const hay=productHaystack(p);const name=textOf(p.name||p.title).toLowerCase();const all=terms.every(t=>hay.includes(t));const score=(name===q?100:0)+(name.includes(q)?30:0)+terms.reduce((n,t)=>n+(hay.includes(t)?1:0),0);return{p,all,score}}).filter(x=>x.all&&x.score>0).sort((a,b)=>b.score-a.score)[0]?.p||null};const processNavigation=(text:string,already=false)=>{const regex=/\[(NAV|EXT):([^:\]]+)(?::([^\]]+))?\]/gi;let done=already,m;while((m=regex.exec(text))){if(done)continue;const kind=m[1].toUpperCase(),action=m[2].toUpperCase(),param=m[3]?.trim();if(kind==='NAV'){if(action==='HOME')navigate('/');else if(action==='PRODUCTS')navigate('/products');else if(action==='CART')navigate('/cart');else if(action==='LOGIN')navigate('/auth');else if(action==='PROFILE')navigate('/profile');else if(action==='CONTACT')navigate('/contact');else if(action==='FAQ')navigate('/support?tab=faq');else if(action==='PRODUCT'&&param){const p=liveProducts.find((x:any)=>String(x.id||x._id)===param);if(p)navigate(`/product/${p.id||p._id}`);else continue}else continue;done=true}else if(kind==='EXT'&&param&&/^https?:\/\//i.test(param)){window.open(param,'_blank','noopener,noreferrer');done=true}}return done};const handleSend=async(preset?:string)=>{const userMessage=String(preset??input).trim();if(!userMessage||isLoading)return;setInput('');setMessages(prev=>[...prev,{role:'user',text:userMessage}]);setIsLoading(true);let immediateNavigation=false;const q=userMessage.toLowerCase();if(/\b(login|sign in|signin)\b/i.test(q)){navigate('/auth');immediateNavigation=true}else if(/\bprofile\b/i.test(q)){navigate('/profile');immediateNavigation=true}else if(/\bfaq|faqs\b/i.test(q)){navigate('/support?tab=faq');immediateNavigation=true}else{const p=findProduct(q);if(p){navigate(`/product/${p.id||p._id}`);immediateNavigation=true}}try{const history=messages.slice(-4).map(m=>({role:m.role==='user'?'user':'model',text:m.text}));const compactProducts=liveProducts.map((p:any)=>({id:p.id||p._id,name:textOf(p.name||p.title,'Product'),price:convertCurrency(Number(p.price||0),(String(p.currency||'USD').toUpperCase() as any),currency),currency,image:textOf(p.image||p.images?.[0]),category:textOf(p.category),description:textOf(p.description).slice(0,240),colors:Array.isArray(p.colors)?p.colors.slice(0,8):[],sizes:Array.isArray(p.sizes)?p.sizes.slice(0,8):[],stock:Number(p.stock??0),isNewArrival:Boolean(p.isNewArrival),isFeatured:Boolean(p.isFeatured),isTrending:Boolean(p.isTrending)}));const response=await axios.post('/api/ai/stylist',{message:userMessage,history,products:compactProducts,siteConfig:{header:SiteConfig?.header,footer:SiteConfig?.footer,pages:SiteConfig?.pages,settings:SiteConfig?.settings},currency},{timeout:10000});const aiText=textOf(response.data?.text,'I could not confirm enough live store information to answer that.');processNavigation(aiText,immediateNavigation);let displayText=aiText.replace(/\[(?:NAV|EXT):[^\]]+\]/gi,'').trim();if(!displayText)displayText=immediateNavigation?'Done — I opened the live destination for you.':'I could not find enough current store information to answer that.';setMessages(prev=>[...prev,{role:'ai',text:displayText}]);speak(displayText)}catch(error){console.error('AI Assistant error:',error);setMessages(prev=>[...prev,{role:'ai',text:'The live stylist is taking a moment. Please try again.'}])}finally{setIsLoading(false)}};return <><motion.button initial={{scale:0,opacity:0}} animate={{scale:1,opacity:1}} whileHover={{scale:1.06}} whileTap={{scale:.94}} onClick={()=>setIsOpen(true)} className="fixed bottom-6 right-6 z-[60] bg-[#050505] text-white p-3 rounded-full shadow-[0_20px_50px_rgba(0,0,0,.3)] border border-brand-gold/30" aria-label="Open AI assistant"><img src={BRAND_LOGO_URL} alt="DENFIT AI" className="w-9 h-9 object-contain rounded-full bg-white"/></motion.button><AnimatePresence>{isOpen&&<motion.div initial={{opacity:0,scale:.96,y:20}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.96,y:20}} className="fixed bottom-20 right-4 md:right-8 z-[100] w-[calc(100%-2rem)] md:w-[470px] h-[680px] max-h-[82vh] bg-white shadow-2xl flex flex-col overflow-hidden border border-brand-gold/20 rounded-2xl"><div className="bg-[#080808] text-white p-4 flex justify-between items-center border-b border-brand-gold/30"><div className="flex items-center gap-3"><img src={BRAND_LOGO_URL} alt={BRAND.name} className="w-10 h-10 object-contain rounded-lg bg-white"/><div><div className="font-serif font-bold tracking-[.12em] text-sm">AI STYLIST</div><div className="text-[9px] uppercase tracking-[.22em] text-brand-gold">{BRAND.name} · Live Concierge</div></div><button onClick={()=>setIsAudioEnabled(v=>!v)} className={`p-1.5 rounded-full ml-1 ${isAudioEnabled?'bg-brand-gold text-black':'text-white/50 hover:bg-white/10'}`}>{isAudioEnabled?<Volume2 size={14}/>:<VolumeX size={14}/>}</button></div><button onClick={()=>setIsOpen(false)} aria-label="Close"><X size={20}/></button></div>{!hasUserMessage&&<div className="hidden lg:block px-4 pt-4 bg-[#F8F5EE] border-b border-brand-dark/5"><div className="flex items-end justify-between mb-3"><div><div className="text-[9px] font-bold uppercase tracking-[.24em] text-brand-gold">Curated live picks</div><div className="text-sm font-serif font-bold text-brand-dark mt-1">Trending now</div></div><span className="text-[9px] uppercase tracking-widest text-gray-400">Tap to explore</span></div><div className="grid grid-cols-4 gap-2 pb-4">{trending.map((p:any)=><button key={String(p.id||p._id)} onClick={()=>navigate(`/product/${p.id||p._id}`)} className="text-left group"><div className="aspect-[3/4] rounded-lg overflow-hidden bg-white border border-brand-dark/10"><img src={textOf(p.image||p.images?.[0])} alt={textOf(p.name||p.title,'Product')} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"/></div><div className="text-[9px] font-semibold text-brand-dark truncate mt-1.5">{textOf(p.name||p.title,'Piece')}</div><ArrowUpRight size={11} className="text-brand-gold mt-0.5"/></button>)}</div></div>}<div className="flex-grow overflow-y-auto p-4 space-y-4 bg-white">{messages.map((msg,idx)=><div key={idx} className={`flex ${msg.role==='user'?'justify-end':'justify-start'}`}><div className={`max-w-[88%] p-3.5 text-sm whitespace-pre-line leading-relaxed ${msg.role==='user'?'bg-[#080808] text-white rounded-2xl rounded-br-sm':'bg-[#F8F5EE] text-brand-dark border border-brand-dark/5 rounded-2xl rounded-bl-sm'}`}>{textOf(msg.text)}</div></div>)}{isLoading&&<div className="flex justify-start"><div className="bg-[#F8F5EE] p-3.5 rounded-2xl flex items-center gap-2 text-xs text-gray-500"><Loader2 size={17} className="animate-spin text-brand-gold"/> Curating from the live collection…</div></div>}<div ref={messagesEndRef}/></div><div className="px-4 pt-3 bg-white border-t border-brand-dark/5"><div className="flex gap-2 overflow-x-auto pb-3 scrollbar-hide">{!hasUserMessage&&quickSuggestions.map(s=><button key={s} onClick={()=>handleSend(s)} disabled={isLoading} className="shrink-0 rounded-full border border-brand-dark/10 bg-[#F8F5EE] px-3 py-1.5 text-[10px] font-semibold tracking-wide text-brand-dark hover:border-brand-gold hover:text-brand-gold disabled:opacity-50">{s}</button>)}</div><div className="pb-4 flex gap-2"><input type="text" value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')handleSend()}} placeholder="Ask your stylist…" className="flex-grow text-sm border border-brand-dark/10 rounded-xl px-3 py-2.5 focus:outline-none focus:border-brand-gold bg-white"/><button onClick={()=>handleSend()} disabled={isLoading} className="w-10 h-10 rounded-xl bg-[#080808] text-brand-gold flex items-center justify-center hover:bg-brand-gold hover:text-black disabled:opacity-50" aria-label="Send"><Send size={18}/></button></div></div></motion.div>}</AnimatePresence></>}
